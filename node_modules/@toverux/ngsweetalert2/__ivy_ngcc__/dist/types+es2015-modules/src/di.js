import { OpaqueToken } from '@angular/core';
export var SWAL_DEFAULTS = new OpaqueToken('SWAL_DEFAULTS');
export function swalDefaultsProvider(options) {
    if (options === void 0) { options = {}; }
    return {
        provide: SWAL_DEFAULTS,
        useValue: options
    };
}
