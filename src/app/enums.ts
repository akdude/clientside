export enum LocalStorageKeys {
    Token = 'token',
    User = 'user',
    AdminUser = 'adminUser'
}

export enum MediaType {
    Image = 1,
    Audio = 2,
    Video = 3
}

export enum CategoryParamTypes {
    Text = 'text',
    Options = 'options',
    Radio = 'radio'
}